{
  "name": "microbit-dev",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "pip install --no-cache-dir uflash notebook ipykernel",
  "settings": {
    "terminal.integrated.defaultProfile.linux": "bash"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter"
      ]
    }
  }
}
